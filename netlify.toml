# Netlify build configuration for the React frontend

[build]
  command = "npm run build"
  publish = "dist"

# SPA routing — send all requests to index.html so React Router handles them
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
